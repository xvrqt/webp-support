const fs = require('fs');
const should = require('should');
const nightmare = require('nightmare');

// const webp_supprt = require('../index.js');

describe('webp-support', () => {
	it("Should return that there is no WebP support", (done) => {
		/* Not implemented */
		console.log("Testing is not implemented.")
		done();
	});

	/* Check that errors are handled correctly */
	it("Should return there is WebP support", (done) => {
		/* Not implemented */
		console.log("Testing is not implemented.")
		done();
	});
});

describe('update-classes', () => {
	it("Should update the class of the body tag - using a PNG", (done) => {
		/* Not implemented */
		console.log("Testing is not implemented.")
		done();
	});

	/* Check that errors are handled correctly */
	it("Should update the class of the body tag - using a WebP", (done) => {
		/* Not implemented */
		console.log("Testing is not implemented.")
		done();
	});
});